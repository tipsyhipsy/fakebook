<h1>Posts#index</h1>

<%= link_to '新規作成', new_post_path %>
<table>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.content %></td>
      <td><%= post.image_cache %></td>
      <td><%= link_to '詳細', post_path(post.id) %></td>
      <td><%= link_to '編集', edit_post_path(post.id) %></td>
      <td><%= link_to '削除', post_path(post.id), method: :delete, data:{confirm: "削除しますか？"} %></td>
    </tr>
  <% end %>
</table>